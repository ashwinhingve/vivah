// Single implementation lives in components/shared/StaggerList.client — this
// path is kept as an alias so both import conventions resolve to one component.
export { StaggerList } from '@/components/shared/StaggerList.client';
