# Smart Shaadi — Audit Resolution Log

> Fix pass for `AUDIT-TRIAGE-AND-FIX-PLAN.md` (PASS-0…PASS-5). Branch:
> `fix/audit-2026-batches`. One row per resolved item: root cause → fix →
> verification evidence → commit. Regression baseline (frozen): API 1388 ·
> ai-service 452 · web 24 · mobile 208 · Playwright 7/23.

| Item | Sev | Root cause | Fix | Verification | Commit |
|---|---|---|---|---|---|
| P1-001 | P1 | 4 sklearn bundles loaded via bare `joblib.load()`; exists()-only fallback never regenerates a present-but-incompatible `.pkl`; unbounded `numpy>=2.0` | `_load_or_retrain()` try/except → retrain-and-reload in dpi/faq/reputation/stay; pins capped `numpy<3`, `scikit-learn<2`, `joblib<2` | pytest 452 (unchanged); all 4 diffs reviewed | `c85d12f` |
| A4-06 | P2 | FastAPI OpenAPI `title="VivahOS AI Service"` brand leak | → `"Smart Shaadi AI Service"` | grep confirms no VivahOS in ai-service; pytest 452 | `c85d12f` |
| A4-01 | P0 | Booking invoice PDF branded "VivahOS" / footer "Powered by VivahOS" / dispute `support@vivah.os` (non-existent domain) | Header→"Smart Shaadi", footer→"Smart Shaadi — National Marriage Ecosystem", dispute→`support@smartshaadi.co.in` | Rendered real PDF; header/footer/contact confirmed via pdftotext; api type-check exit 0 | `730cd6d` |
| A2-02 | P1 | `formatInr` emitted `₹` (U+20B9) into default-Helvetica PDFKit → black box on customer invoice | `formatInr` now emits grouped ASCII `Rs. 1,25,000.00`, never the glyph | `pdftotext -layout \| grep ■` → empty on rendered invoice | `730cd6d` |
| U1 | P1 | MatchCard name/city white text unreadable on photo-less (fallback) cards | Branch overlay on `hasPhoto`: white-on-gradient w/ photo, `text-text`/`text-muted` on fallback | Browser QA 2026-07-29 @375+1440: `/feed` (ProfileCard, always-on scrim) PASS — worst-case 3.2:1 name (AA-large) / 4.9:1 meta, screenshots; `/dashboard` (MatchCard) fallback name PASS 11.65:1 — but city line **FAIL 1.06:1** → new finding U1-b below | `bde0c8c` |
| U2 | P1 | `/vendor-dashboard/orders` tabs built key `tab${VALUE}` (tabALL) vs catalog camelCase (tabAll) → raw keys shown | Normalize key case in `t()` call | Browser QA 2026-07-29 as VENDOR `qa-ven-01` @375+1440: tabs render All/Pending/Shipped/Delivered, zero raw `tab*` keys in DOM, filter click works, console clean | `bde0c8c` |
| U3 | P1 | `/profile/personal` dob + 6 selects had no programmatic label | Added `id` + `htmlFor` on all 7 controls | Browser QA 2026-07-29, a11y-tree probe (`el.labels`, not htmlFor presence): 6/7 controls have accessible names; `heightIn` has **none** (the "Height" label targets only `heightFt`) → new finding U3-b below | `bde0c8c` |
| A3-01 | P1 | Presigned R2 URLs rendered via `next/image` (expiry + optimizer defeats presigning) | Detect presigned (X-Amz-Signature/Expires) → plain `<img>`; next/image for other hosts | Browser QA 2026-07-29: non-presigned branch verified live (profile w/ photos: 6/6 imgs via `/_next/image`, 0 broken, both widths); `resolvePhotoUrl` bare-key path presign-safe by design (`/__media` 302 → fresh signature per request). Presigned branch NOT exercisable locally (mock R2 emits unsigned `__mock-r2` URLs) and has no unit test → verification gap in deferred table | `bde0c8c` |
| A2-08 | P2 | Web currency mapped CAD/AUD/SGD to native locales (ambiguous `$`) + blind `$→US$` ascii replace (mislabels CAD) | Mirror API: en-US + currency-keyed ASCII prefixes (CA$/A$/SGD) | web type-check + 24 tests pass | `bde0c8c` |
| A2-01 | P1 | Mobile `billing.tsx` divided rupee `plan.amount` by 100 (₹499→₹5); unit test masked it with a paise mock | Drop `/100`; fix test mock 49900→499; correct api-client doc | mobile billing test 3/3 pass; API return unit re-traced (rupees) | `93d59f9` |
| A2-03 | P1 | Razorpay SDK v2.9.6 strips non-allowlisted headers → cannot send `X-Razorpay-Idempotency-Key`; a retried lost/5xx response re-executed refund/transfer | `razorpayPost` raw-REST path sends the key; money-out calls route through it when a deterministic key is supplied (reused across retries → Razorpay dedupes). Caller threading in Batch 3 | api type-check; razorpayIdempotency test 3/3 | `ab94110` |
| A2-04 | P2 | Wallet credit/debit float read-modify-write, no lock → lost update | Atomic SQL `balance = balance ± amount`; debit guard `balance >= amount` in the same UPDATE WHERE | wallet test 3/3 (mock models RETURNING) | `ab94110` |
| A2-05 | P2 | Topup dedup SELECT outside credit txn (TOCTOU); whole-rupee credit `Math.round(paise/100)` | Dedup inside txn + `SELECT … FOR UPDATE` wallet-row lock; credit exact `amount/100` | api type-check; payments suite | `ab94110` |
| A2-06 | P2 | GST computed by two byte-identical copies (invoiceService + b2b) that could drift; persisted GST untested | One shared `computeGst`; both callers delegate | gst test 5/5 (rounding + residual sum) | `ab94110` |
| A2-07 | P2 | PDF `formatRupees` had no lakh/crore grouping | Group integer part on top of `toFixed(2)` (rounding unchanged) | reports test 17/17 (grouping assertion updated) | `ab94110` |
| P1-S1 | P1 | `GET /ai/fii/score/:profileId` selected `profiles.userId` but never checked it → any user could read any profile's FII (feed hands out profileIds) | Enforce owner-or-ADMIN; 404 on non-owner (no relationship leak) | api type-check; mirrors sibling optimizer/readiness guards | `d22de27` |
| P1-S2 | P1 | `cancelBooking` + `requestRefund` read-then-update money state (safe only via external backstops) | Local atomic CAS: escrow `HELD→REFUND_PENDING` guarded + RETURNING (only winner refunds); payment claimed before Razorpay; deterministic idempotency key threaded | bookings+payments suites 117/117; api type-check | `d22de27` |
| P2-1 / P2-6 | P2 | Audit-chain hash used `JSON.stringify(payload)` → diverged on jsonb round-trip (false `CHAIN TAMPERED`); 3 duplicated copies | One shared `lib/auditHash.auditContentHash`, canonical (key-sorted) serialization; all 3 sites use it | api type-check; admin/jobs suites | `d22de27` |
| P2-3 | P2 | Virtual-date NO_SHOW sweep updated `WHERE id IN(…)` with no status guard → could flip a rated date | Re-assert `status=CONFIRMED` + unrated in the UPDATE (first-writer) | video suite 32/32 | `d22de27` |
| P2-5 | P2 | Pino redact lacked response `set-cookie` (session token) | Added `res.headers["set-cookie"]` | api type-check | `d22de27` |
| U4 | P1 | EVENT_COORDINATOR 404'd on assigned weddings — dashboard-linked handlers used OWNER-only `resolveOwnedWedding` while schema+guard already model COORDINATOR access | `resolveOwnedWedding(…, minRole)`: owner fast-path, then access guard; 11 operational handlers relaxed to VIEWER/EDITOR; owner-only ops unchanged | weddings suites 46/46; api type-check | `055c423` |
| A4-03 | P1 | Mock OTP override honoured whenever `USE_MOCK_SERVICES` — under the `ALLOW_MOCK_SERVICES_IN_PROD` escape hatch, seeded QA/ADMIN/SUPPORT accounts were loginable in prod with a known code | Request-time guard: sendOTP mock override throws when `NODE_ENV=production`; phone-change response never echoes the code in prod | auth+env suites 19/19; api type-check | `a40a14f` |
| A4-05 | P1 | `NEXT_PUBLIC_API_URL ?? 'http://localhost:4000'` inlined across 184 web files, no build guard | Central `lib/api-url.ts` (throws on a real prod deploy `VERCEL_ENV=production` when unset; localhost fallback for local/CI/preview); codemod-migrated all 184 sites onto `API_URL` (imports after directives, no self-ref consts); fixed ChatView `/api/v1` drop | web type-check + `next build` pass; 0 leftover fallbacks | `45d9ab4` |
| P2-2 | P2 | Non-money read-then-update transitions (decline/withdraw, subscription webhook states, completeBooking) | Optimistic CAS: `AND status=<expected>` in the UPDATE WHERE; subscription terminal case gates `downgradeUserTier` on the CAS winner | matchmaking+payments+bookings 226/226; api type-check | `5b50757` |
| P2-4 | P2 | `blockUser`/`unblockUser` (safety actions) + the virtual-date lifecycle sweep left no chained audit trail | Block audit (`PROFILE_BLOCKED`) in `5b50757`; **completed** in `51d95e3` — added enum values `PROFILE_UNBLOCKED`/`VIRTUAL_DATE_EXPIRED`/`VIRTUAL_DATE_NO_SHOW` + migration 0041 (schema unfreeze approved), wired unblock + sweep to correct events (best-effort). Apply 0041 on deploy | api type-check; video+matchmaking 70/70 | `5b50757`, `51d95e3` |
| A4-09 | P2 | Mobile baked `EXPO_PUBLIC_MEDIA_URL=media.smartshaadi.co.in` but that host was never set up (DNS on Hostinger; R2 custom domains need the zone on Cloudflare) → all mobile media 404'd | Point mobile (`eas.json` profiles + `env.ts` fallback) at the `pub-…r2.dev` public bucket the API's `R2_PUBLIC_URL` already serves — both sides aligned, no DNS needed. **Interim**: r2.dev is rate-limited (fine pre-launch) — branded-domain follow-up in deferred table | config-only; matches live API `R2_PUBLIC_URL` | `78bb973` |
| P2 bare-throws | P2 | 103 bare `throw new Error()` in api business logic collapsed 403/404/409/429 conditions into opaque 500s (middleware ignored error intent) | Landed in 3 steps: 11 via existing module classes (`7dccb76`); error middleware honors thrown `.status`+`.code` + shared `lib/errors.ts` `AppError` (`93ba739`); remaining **80 converted per-module** (`0126aef`) — payments cluster via existing `err()` factory (dispute adopts subscriptions pattern), kyc/auth/msg91 provider stubs→503, OTP rate-limit→429, mock-disabled→403, DB write failures→coded 500; weddings router branches on `.code` not message text. `lib/*` pure-utility + Sentry-test throws intentionally left bare | `turbo type-check --force` clean; api vitest **1396/1396** (up from frozen 1388 baseline — typed-error tests added) | `7dccb76`, `93ba739`, `0126aef` |
| A4-08 | P2 | `MIGRATIONS-PENDING.md` stale: documented ≤0029, plus two duplicate "0027 … PENDING" headers contradicted by the "APPLIED 2026-06-01" log below them | Doc refresh: 0030–0041 documented (id/source/scope, additive+idempotent flags); stale headers removed; freeze note (schema frozen at 0040; 0041 enum-only forward-only); apply-state marked operator-verify-required (prod `__drizzle_migrations` baseline-seeded only through 0029); cross-ref `docs/db/journal-drift.md` | doc-only; grep for PENDING no longer false-hits | `1b9681b` |
| A4-04 | P1 | Mocked OTP/payment surfaces carried no test-mode label; only signal was web `DemoPill` gated on the unrelated `NEXT_PUBLIC_DEMO_MODE`; mobile had no exposed mock flag at all | Single API source: `GET /platform-settings/public` now returns `isMockMode = env.USE_MOCK_SERVICES` (can never desync). Web `getPlatformSettings()` helper (React `cache()`, SSR-safe, fail-closed→false) threads into verify-otp + store checkout + billing (billing re-pointed off `process.env`); mobile `usePlatformSettings()` TanStack hook badges OTP + payments screens; `EXPO_PUBLIC_MOCK_MODE` offline fallback in dev/preview EAS profiles only (not production) | `turbo type-check --force` clean (api+web+mobile); live API returns `{isMockMode:true}`; **web badge visually verified 2026-07-29** — "Test Mode — no real charge will be made" renders on verify-otp, `/store/checkout`, `/settings/billing` @375+1440, no overflow, console clean (mobile device render still deferred) | `e053ca9` |
| PASS-0 #5/#6 | doc | ROADMAP overstated BetterStack as "✅ Configured" (docs-only; admin TODO placeholders) and NBFC-lending + insurance as "Built, gated" (static `MOCK_OFFERS`/`MOCK_QUOTES` shells; the `*_LIVE=true` path throws) | ROADMAP corrected to scaffold-only / mock-shell wording per PASS-0 doc-truth findings | doc-only | `7309bb7` |
| A4-06 (CORS) | P2 | `apps/api/src/lib/cors.ts` preview regexes/globs (and 10+ `cors.test.ts` assertions) still anchored to the `vivah-web` codename alias | Swapped to `smart-shaadi-web` (comment, 2 regexes, 3 Better-Auth globs, tests); added regression assertion that the retired `vivah-web.vercel.app` is now rejected. Ops must rename the Vercel project to `smart-shaadi-web` — until then previews have no CORS access (prod domain unaffected) | cors vitest 10/10; full api suite in final regression | uncommitted (2026-07-29 session) |
| 0030–0041 apply | P2 | Prod apply-state of hand-authored migrations unconfirmed; `drizzle.__drizzle_migrations` stopped at 0029 | Verified live 2026-07-29 (psql/WSL → Railway PG 18.3): every 0030–0041 object already present (pgvector, `vector(768)`+HNSW, all tables/indexes/enums). Idempotence re-run of 0031–0041 no-oped except 0038's guarded `vendors.city_id` backfill (linked 12 NULL rows — designed; snapshot + rollback kept). Ledger seeded through 0041 (`scripts/db/reconcile-ledger-0030-0041-2026-07-29.sql`, 42 rows) on prod + local. File-side journal drift still open — `docs/db/journal-drift.md` | per-file re-run OK; ledger no-op on re-run; probes in MIGRATIONS-PENDING.md | `reconcile-ledger-0030-0041-2026-07-29.sql` |

## New findings — browser QA 2026-07-29 (OPEN, not fixed; verify-only pass)

| Item | Sev | Finding | Evidence | Suggested fix |
|---|---|---|---|---|
| U1-b | P1 | `MatchCard.tsx:119` fallback city line uses `text-muted` — but the `muted` token is a **surface** tint (`#F8F5F0`), not the muted text color (`muted-foreground` `#6B6B76`). City is invisible on the InitialAvatar pastel | Measured 1.06:1 vs effective backdrop rgb(230,241,241); screenshots @375+1440 show "Mumbai" unreadable while the name (11.65:1) is fine | One-token change: `text-muted` → `text-muted-foreground` on the city `<p>` (and audit other `text-muted` uses — same trap) |
| U3-b | P2 | `/profile/personal` `heightIn` select has no accessible name — the "Height" `<label htmlFor="heightFt">` labels only the feet select | a11y probe: `heightIn.labels.length === 0`, no `aria-label`/`aria-labelledby`; other 6 controls pass | `aria-label="Height (inches)"` on `heightIn` (+ the page also has an unlabeled text input and textarea outside U3 scope, worth the same sweep) |
| RAG-merge tests | P2 | 4 api tests fail on HEAD: `vendors/__tests__/approval.service.test.ts` — merge `56419cc` (`0f7ff98`, assistant RAG) added a `queueKnowledgeIndexing` auto-index call to approval.service.ts without updating the test's `vi.mock` of `infrastructure/redis/queues.js` | api vitest 2026-07-29: 4 failed / 1413 passed; error "No queueKnowledgeIndexing export is defined on the mock"; pre-dates this session (not caused by audit fixes) | Add `queueKnowledgeIndexing: vi.fn()` to that test file's queues mock |

## Final regression — 2026-07-29 (vs frozen baseline API 1388 · ai 452 · web 24 · mobile 208 · Playwright 7/23)

| Check | Result | vs baseline |
|---|---|---|
| `turbo type-check --force` | 13/13 packages clean (cold, 2m26s) | ✅ |
| api vitest | 1413 passed / **4 failed** (1417) | ⚠️ the 4 = pre-existing RAG-merge finding above; everything the audit touched is green (cors 10/10) |
| ai-service pytest | 462 passed | ✅ (452 → 462) |
| web vitest | 33 passed | ✅ (24 → 33) |
| mobile jest | 215 passed | ✅ (208 → 215) |
| Playwright e2e (first real re-run; 2 projects × 31 cases) | 29 passed / 25 failed / 8 skipped (11.1m) | ✅ above the 7-passing baseline. Failures are (a) 60s navigation timeouts against the cold turbopack dev server (auth/roles/matching helpers), and (b) axe-core `color-contrast` serious violations on `/en/dashboard` — **independently corroborating U1-b**. No OTP-429/typed-error signatures — no evidence any audit fix regressed e2e |

## Deferred / ops (not code-fixable in this pass)

| Item | Sev | Reason deferred | Action needed |
|---|---|---|---|
| A4-06 (alias) | P2 | Code half done 2026-07-29 (cors.ts + tests now expect `smart-shaadi-web`); the Vercel project itself is still named `vivah-web` — previews lose prod-API CORS until renamed (prod domain unaffected) | Ops: rename the Vercel project/alias to `smart-shaadi-web` (Vercel dashboard → project settings); no code change needed after |
| A4-09 (follow-up) | P2 | `78bb973` points mobile at the `pub-…r2.dev` public bucket — **rate-limited by Cloudflare**, acceptable pre-launch only | Ops before public launch: move the zone to Cloudflare, add R2 custom domain `media.smartshaadi.co.in`, set `R2_PUBLIC_URL`/`EXPO_PUBLIC_MEDIA_URL` to it, revert `78bb973`, rebuild the mobile app |
| A4-04 (mobile QA) | P1 | Web badge verified 2026-07-29 (see resolved row); mobile OTP/payments badge still never device-rendered (no local Android toolchain/emulator) | Device/EAS-build QA: confirm badge on mobile OTP + payments screens |
| A3-01 (presigned QA) | P2 | Presigned branch of `ImageWithFallback` unverifiable locally — mock R2 emits unsigned URLs; no unit test covers `isPresignedUrl` → plain-`<img>` choice | Either a staging check with `R2_LIVE=true` (confirm X-Amz imgs render via plain `<img>`) or add a small unit test for the presigned branch |
