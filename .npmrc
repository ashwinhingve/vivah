# Vercel sets NODE_ENV=production during build, which makes pnpm skip
# devDependencies. The Next.js build on Vercel needs tsc + turbo at
# install time, so force dev deps in regardless of NODE_ENV.
production=false
